# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                                   controllers.Soccer.index()
GET        /soccerWs                            controllers.Soccer.soccerWs()
GET        /assets/javascripts/soccer.js        controllers.Soccer.soccerJs()

# Send events
GET        /send                               controllers.Soccer.send()
POST       /send                               controllers.Soccer.sendEvent()

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file                       controllers.Assets.at(path="/public", file)
